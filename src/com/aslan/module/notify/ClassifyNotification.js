import Notification from "./Notification";

export default class ClassifyNotification extends Notification {

	addNotifier(name, notifier, global) {

	}

    removeNotifier(name, notifier, global) {

	}

	notify(event) {

	}
}